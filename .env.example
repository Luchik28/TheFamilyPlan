# Vercel Postgres connection string.
# In Vercel: add a Postgres database (Storage tab) — these are injected
# automatically on deploy. For local dev, run `vercel env pull .env.local`
# to populate this, or paste your connection string below.
POSTGRES_URL="postgres://user:password@host:5432/dbname?sslmode=require"
